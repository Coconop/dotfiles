# tmux colors
set -s default-terminal "tmux-256color"
set -as terminal-overrides ",*-256color:Tc"

### Shortcut to refresh config
unbind r
bind r source-file ~/.tmux.conf\; display-message "sourcing ~/.tmux.conf"

### Enable Vi keys
set-window-option -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

### Copy/Paste
# Enter Copy mode: Ctrl+b [
# Begin selection FULL WIDTH: Ctrl+space or v
# OR Rectangle selection: Ctrl+v, THEN Space
# Copy selection: y or Enter (selection is copied to OS clipboard)
# Paste buffer: Ctrl+b ]
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "cat > /dev/clipboard"
bind-key -T copy-mode-vi Enter send -X copy-pipe-and-cancel "cat > /dev/clipboard"

# Toggle synchronize all panes in a window
bind C-x set-window-option synchronize-panes\; display-message "synchronize-panes is now #{?pane_synchronized,on,off}"

# Shall be empty if we are not in WSL
source-file ${HOME}/.tmux_wsl.conf

# Show panes until one is selected
bind -T prefix q display-panes -d 0

# Ensure split panes open in current path
bind % split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"
# open new windows in the current path
bind c new-window -c "#{pane_current_path}"

set -g mouse on
set -g base-index 1                 # start indexing windows at 1 instead of 0
setw -g pane-base-index 1           # same for pane (window option)
set -g detach-on-destroy off        # don't exit from tmux when closing a session
set -g escape-time 0                # zero-out escape time delay
set -g history-limit 1000000        # increase history size (from 2,000)
set -g renumber-windows on          # renumber all windows when any window is closed
set -g set-clipboard on             # use system clipboard

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# PIMP MY STATUS BAR

# Nord Palette
set-option -g @PolarNight0 "#2e3440"
set-option -g @PolarNight1 "#3b4252"
set-option -g @PolarNight2 "#434c5e"
set-option -g @PolarNight3 "#4c566a"
set-option -g @Cloudy3_5 "#7C8593"
set-option -g @SnowStorm4 "#d8dee9"
set-option -g @SnowStorm5 "#e5e9f0"
set-option -g @SnowStorm6 "#eceff4"
set-option -g @Frost7 "#8fbcbb"
set-option -g @Frost8 "#88c0d0"
set-option -g @Frost9 "#81a1c1"
set-option -g @Frost10 "#5e81ac"
set-option -g @Aurora11 "#bf616a"
set-option -g @Aurora12 "#d08770"
set-option -g @Aurora13 "#ebcb8b"
set-option -g @Aurora14 "#a3be8c"
set-option -g @Aurora15 "#b48ead"

set-option -g @zoom_sign "(+)"
# don't rename windows automatically
set -g allow-rename off
# Status bar on top to avoid conflicts with vim one
set -g status-position top
# Global status-line style
set-option -g status-style "bg=#{@PolarNight1},fg=#{@SnowStorm4}"
# What's on the left side
set-option -g status-left-length 100
set-option -g status-left "[#[bg=#{@PolarNight1},fg=#{@Aurora14},bold]\
#S\
#[bg=#{@PolarNight1},fg=#{@SnowStorm4}]] "
# What's on the right side
set-option -g status-right-length 100
set-option -g status-right "#[bg=#{@PolarNight1},fg=#{@Frost10}] \
#(whoami)\
#[fg=#{@Frost8}]@#(hostname -I | awk '{print $1}')"
# Inactive window(s) style
set-window-option -g window-status-format "\
#[fg=#{@Cloudy3_5}]#I:#W"
# Active window style
set-window-option -g window-status-current-format "\
#[bg=#{@SnowStorm4},fg=#{@PolarNight2},bold]#I\
#[bg=#{@PolarNight3},fg=#{@SnowStorm4}]:#W\
#{?window_zoomed_flag,#{@zoom_sign},}"
# Warnings etc
set-option -g message-style "bg=#{@Aurora13},fg=#{@PolarNight1}"
# Session selection
set-option -g mode-style "bg=#{@Frost8},fg=#{@PolarNight1}"
# Inactive Pane Border (color and style)
set-option -g pane-border-style "fg=#{@PolarNight3}"
# Active Pane Border (color and style)
set-option -g pane-active-border-style "fg=#{@Aurora14},bold"

